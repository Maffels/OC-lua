{
  --default installation path
  path="/usr",
  --Additional repositories and packages go here, for correct package syntax, check https://github.com/OpenPrograms/Vexatos-Programs/blob/master/oppm/etc/example-config.cfg
  repos={
    ["Maffels/OC-lua"] = {
      ["OreCraft"] = {
        files = {
          ["master/OreCraft/OreCraft.lua"] = "/bin/",--"/" 
          [":master/lib"] = "/lib/"
        },
        name = "OreCraft Working-Branch",--This is for "oppm info"
        description = "A work in progress lua learning experience",--This is for "oppm info"
        authors = "Maffraff",--This is for "oppm info"
        note = "Very much not yet Complete",
        hidden = false, -- Add this optional line to make your package not visible in "oppm list", useful for custom dependency libraries
        repo="tree/master/somefolder" --Used by the website. This is where the package will link to on the website
      },

    },

  }
}
